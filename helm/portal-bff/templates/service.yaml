apiVersion: v1
kind: Service
metadata:
  name: portal-bff
  namespace: compass
spec:
  selector:
    app: portal-bff
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.port }}
  type: ClusterIP
